COMPONENT = NodeAppC

ANT_ROUTE_UPDATE_TIME=8000
MCU_SLEEP_TIME=0
MCU_ACTIVE_TIME=100

######################

PFLAGS += -DZIGBEM  -DUSE_BOARD_LED
PFLAGS += -DANT_ROUTE_UPDATE_TIME=$(ANT_ROUTE_UPDATE_TIME)
PFLAGS += -DMCU_SLEEP_TIME=$(MCU_SLEEP_TIME)     -DMCU_ACTIVE_TIME=$(MCU_ACTIVE_TIME)

######################

include $(MAKERULES)
